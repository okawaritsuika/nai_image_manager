# v1.1.0 EXE Patch Notes HTML Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add one standalone, inline-styled Korean HTML patch note for `v1.1.0-exe`.

**Architecture:** Copy the visual vocabulary of the existing v1.0.0 Lite HTML while using the verified v1.1.0 Markdown content as the source of truth. Keep every style inline and add no JavaScript, stylesheet, or style block.

**Tech Stack:** HTML5 with inline `style` attributes

---

### Task 1: Create and Verify the Patch Note

**Files:**
- Create: `patch_notes/2026-06-20-v1.1.0-exe-release.html`

- [ ] Create the dark card-based HTML with release metadata, EXE updates, migration, preservation, search index, usage, limitations, verification, and SHA-256 sections.
- [ ] Run `rg -n "<script|<style|stylesheet|javascript:" patch_notes/2026-06-20-v1.1.0-exe-release.html`; expect no matches.
- [ ] Check the file contains `v1.1.0-exe`, the GitHub Release URL, `업데이트 확인`, `TOTAL_CLASSIFIED`, `26개`, and the final SHA-256.
- [ ] Render the local HTML and inspect Korean text, card spacing, borders, wrapping, and links.
- [ ] Commit with `docs: add v1.1.0 EXE HTML patch notes`.
